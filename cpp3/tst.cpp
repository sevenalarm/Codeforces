#include <bits/stdc++.h>
#define ll long long
using namespace std;


int fib(int n) {
    if (n < 3) return 1;
    else return fib(n-1) + fib(n-2);
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

}